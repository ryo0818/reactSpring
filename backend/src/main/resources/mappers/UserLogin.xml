<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.UserLoginRepository">
	<select id="cheakUserLogin" parameterType="string" resultType="String">
		select
		count(*)
		from
		(
		select
		m.mycompanycode
		from
		mycompany m
		where
		m.mycompanycode = #{myCompanyCode}
		) as m
		inner join (
		select
		ru.mycompanycode
		, ru.email
		from
		regUser ru
		where
		ru.email = #{mailAdder}
		) as r
		on r.mycompanycode = m.mycompanycode;

	</select>
</mapper>
