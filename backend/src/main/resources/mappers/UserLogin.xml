<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.UserLoginRepository">
	<select id="cheackLoginUser" parameterType="string" resultType="String">
		select
		count(*)
		from
		reguser
		where reguser.id = #{userId}
		and reguser.email = #{email}
	</select>

	<select id="cheackCompanyCode" parameterType="string" resultType="String">
		select
		count(*)
		from
		mycompany
		where mycompany.mycompanycode = #{companycode}
	</select>
</mapper>
