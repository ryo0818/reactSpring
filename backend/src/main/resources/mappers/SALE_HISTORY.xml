<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.SaleHistoryRepository">

	<insert id="insertSaleStats"
		parameterType="com.example.demo.entity.SaleHistoryEntity">
		INSERT INTO sale_history (
		sale_id,
		user_team_code,
		status_name,
		insert_date_time,
		valid_flg,
		status_id,
		media
		)
		VALUES (
		#{saleId,jdbcType=INTEGER},
		#{userTeamCode,jdbcType=VARCHAR},
		#{statusName,jdbcType=VARCHAR},
		#{insertDateTime,jdbcType=TIMESTAMP},
		#{validFlg,jdbcType=TIMESTAMP},
		#{statusId,jdbcType=INTEGER},
		#{media,jdbcType=VARCHAR}
		)
	</insert>

	<select id="selectAggregatedByUnit"
		resultType="com.example.demo.dto.SaleHistoryAggDto"
		parameterType="com.example.demo.dto.SalesAchievementsDto">
		SELECT DATE_TRUNC('hour', insert_date_time) AS hour_time, status_name, COUNT(*) AS cnt 
		FROM sale_history 
		WHERE user_team_code = #{userTeamCode,jdbcType=VARCHAR}
		AND insert_date_time >= #{searchStartDate,jdbcType=VARCHAR} AND insert_date_time &lt;  #{searchEndDate,jdbcType=VARCHAR}
		GROUP BY
		DATE_TRUNC('hour', insert_date_time),
		status_name
		ORDER BY
		hour_time
	</select>

</mapper>
